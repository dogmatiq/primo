syntax = "proto3";
package primo.test.mutator;

option go_package = "github.com/dogmatiq/primo/internal/test/mutator";

enum Proto3OptionalStatus {
  PROTO3_OPTIONAL_STATUS_UNSPECIFIED = 0;
  PROTO3_OPTIONAL_STATUS_OK = 1;
  PROTO3_OPTIONAL_STATUS_ERR = 2;
}

// Proto3OptionalMessage contains a mix of proto3 optional fields and a real
// oneof, to verify that optional fields are not misidentified as oneof members
// by the mutator generator.
message Proto3OptionalMessage {
  optional int32 num = 1;
  optional string str = 2;
  optional bool flag = 3;
  optional Proto3OptionalStatus status = 4;
  optional Proto3OptionalChild child = 5;

  oneof choice {
    string choice_a = 6;
    int32 choice_b = 7;
  }

  message Proto3OptionalChild {}
}
